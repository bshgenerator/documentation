---
title: Import Entities
---

import { Callout } from 'nextra/components';

# Import Entities

<Callout>
  This endpoint is not available yet, it will be available in the future releases.
</Callout>

Import entities from various formats (CSV, JSON, etc.) into the entity.

```bash
POST /api/entities/:entityName/import?format={{format}}
```
