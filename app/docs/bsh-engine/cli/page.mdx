---
title: CLI
asIndexPage: true
---

# CLI Documentation

## Overview

`bshg` is the BSH Engine Manager CLI tool for managing BSH Engine plugins. It provides commands to install and manage plugins for the BSH Engine platform.

## Installation

<Tabs items={['global', 'local', 'npx']}>
    <Tabs.Tab title="global">
    ```bash
    npm install -g @bshsolutions/cli
    # usage
    bshg [options] [command]
    ```
    </Tabs.Tab>
    <Tabs.Tab title="local">
    ```bash
    npm install -D @bshsolutions/cli
    # usage
    npx bshg [options] [command]
    ```
    </Tabs.Tab>
    <Tabs.Tab title="npx">
    ```bash
    npx @bshsolutions/cli [options] [command]
    ```
    </Tabs.Tab>
</Tabs>

## Usage

<Tabs items={['npm', 'npx']}>
    <Tabs.Tab title="npm">
```bash
bshg [options] [command]
```
    </Tabs.Tab>
    <Tabs.Tab title="npx">
```bash
npx @bshsolutions/cli [options] [command]
```
    </Tabs.Tab>
</Tabs>

<Callout type="">
  - `-V, --version` - Display version number
  - `-h, --help` - Display help for command
</Callout>

### Commands

- bshg
  - [`plugins`](/docs/bsh-engine/cli/plugins)
    - [`install`](/docs/bsh-engine/cli/plugins#install)
    - [`validate`](/docs/bsh-engine/cli/plugins#validate)
    - [`generate`](/docs/bsh-engine/cli/plugins#generate)
  - [`docker`](/docs/bsh-engine/cli/docker)
    - [`setup`](/docs/bsh-engine/cli/docker#setup)
